import{_ as i,o as e,c as a,ai as t}from"./chunks/framework.DQi1ytmF.js";const c=JSON.parse('{"title":"Scaling routes","description":"","frontmatter":{},"headers":[],"relativePath":"guide/routing/scaling.md","filePath":"guide/routing/scaling.md"}'),n={name:"guide/routing/scaling.md"};function h(l,s,p,o,r,d){return e(),a("div",null,[...s[0]||(s[0]=[t(`<div style="display:none;" hidden data-nosnippet>Are you an LLM? You can read better optimized documentation at /craft/guide/routing/scaling.md for this page in Markdown format</div><h1 id="scaling-routes" tabindex="-1">Scaling routes <a class="header-anchor" href="#scaling-routes" aria-label="Permalink to &quot;Scaling routes&quot;">​</a></h1><p><code>ValidateCascadeRoutesFile</code> walks every route in a collection <strong>at the type level</strong>, so a single routes file has a finite budget before TypeScript&#39;s instantiation ceiling. This page is about what happens at that ceiling, and how to organise routes so you never reach it.</p><div class="tip custom-block"><p class="custom-block-title">You don&#39;t need this yet</p><p>If your app has one routes file with a handful of routes, <a href="/craft/guide/routing/setup.html">Setup</a> is enough. Come back when a file grows past a few dozen routes, or when you see <code>TS2589</code>.</p></div><h2 id="large-route-files-—-the-cascade-di-depth-limit" tabindex="-1">Large route files — the cascade DI depth limit <a class="header-anchor" href="#large-route-files-—-the-cascade-di-depth-limit" aria-label="Permalink to &quot;Large route files — the cascade DI depth limit&quot;">​</a></h2><p><code>ValidateCascadeRoutesFile&lt;…, typeof appRoutes&gt;</code> walks <strong>every</strong> route in the collection at the type level. TypeScript caps how deeply it will instantiate a recursive type, so a single collection has a <strong>finite route budget</strong>. Past it (in practice a few dozen routes, sooner if routes carry guards / <code>resolve</code> / <code>handleExceptions</code>), the check overflows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TS2589: Type instantiation is excessively deep and possibly infinite.</span></span>
<span class="line"><span>  app.routes.ts → ValidateCascadeRoutesFile&lt;never, Router, typeof appRoutes&gt;</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Watch out for the knock-on collapse</p><p>A <code>TS2589</code> makes TypeScript abandon that type and fall back to <code>any</code>, which <strong>poisons inference of neighbouring <code>const</code>s in the same file</strong>. The visible symptoms are misleading: <code>craftRoute(...)</code> calls collapse to <code>RouteWithProvidersBuilder&lt;{ path }&gt;</code>, the <code>craftRoutes(...)</code> helpers go missing (<code>Property &#39;injectXxx&#39; does not exist</code>), and <code>craftRouterLink</code> targets type as <code>never</code>. The root cause is the overflowing check, not those routes.</p></div><p><strong>Solution — split into a lazy child collection, and keep its own DI check.</strong> The cascade check reads only the <em>current</em> collection&#39;s metadata; it does <strong>not</strong> descend into <code>loadChildren</code>. So move the extra routes into their own <code>craftRoutes(...)</code> file and reference it via <code>loadChildren</code>. That keeps the parent file under budget — <strong>but a child collection ships with <em>no</em> DI checking unless you add one</strong>, so re-declare the check in the child file to keep DI sound. <a href="/craft/guide/testing/architecture.html#assertroutediproofs">Architecture tests</a> fail if that child proof is missing.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// feature.routes.ts — its own lazy collection</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  craftRoutes,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  craftRoute,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CanRun,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ValidateCascadeRoutesFile,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ng/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Router } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@angular/router&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">featureRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> craftRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;feature&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  craftRoute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    componentDeps: {} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./feature&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GenDeps_Feature</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    loadComponent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./feature&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // guards / resolve / handleExceptions …</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DI safety for THIS collection — \`app.routes.ts\` does NOT cover loadChildren.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Same parent context the parent route runs under: app-level \`Router\` by value,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// no extra named providers.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _CheckFeatureDI</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ValidateCascadeRoutesFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  never</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  Router</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> featureRoutes</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _CanRunFeature</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CanRun</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_CheckFeatureDI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// app.routes.ts — a cheap loadChildren entry, outside the parent&#39;s budget</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;feature&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  loadChildren</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./feature.routes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">m</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> m.featureRoutes),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span></code></pre></div><p>A missing provider in the child collection now surfaces as a TypeScript error <strong>in the child file</strong>, exactly like the main one:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>The SomeService service is not provided in path: &quot;&quot;</span></span></code></pre></div><p>If a single feature is itself large, repeat the split, or break one big collection into several <code>craftRoutes(...)</code> collections each with its own check — every check then validates a smaller slice and stays under the depth limit. The takeaway: <strong>DI is always verified — never drop the check; move it next to the routes it covers.</strong></p><h3 id="why-the-budget-exists-the-mechanism" tabindex="-1">Why the budget exists (the mechanism) <a class="header-anchor" href="#why-the-budget-exists-the-mechanism" aria-label="Permalink to &quot;Why the budget exists (the mechanism)&quot;">​</a></h3><p><code>ValidateCascadeRoutesFile</code> recurses over the route tuple <strong>4 routes per step</strong>, so a file of <code>N</code> routes recurses to depth <code>N / 4</code>. Two distinct TypeScript ceilings are in play:</p><ul><li><strong>Instantiation <em>depth</em></strong> (the <code>TS2589</code> &quot;excessively deep&quot; error). The 4-at-a-time unrolling is what fights this: it quarters the recursion depth, so the wall moves from ~50 routes to a few hundred — but it is still a per-file ceiling.</li><li><strong>Total instantiation <em>count</em></strong>. Each route pays one full <code>RouteCheckedDI</code> instantiation (walking its <code>GenDeps</code>, the <code>missingProvider</code> map, the parent context). The total cost is therefore roughly <strong><code>N × cost-per-route</code></strong>, and a route carrying guards / <code>resolve</code> / <code>handleExceptions</code> costs several times more than a trivial one. This is why &quot;a few dozen&quot; is only a rough figure — the real budget is in route-<em>cost</em>, not route-<em>count</em>.</li></ul><h2 id="scaling-to-hundreds-of-routes" tabindex="-1">Scaling to hundreds of routes <a class="header-anchor" href="#scaling-to-hundreds-of-routes" aria-label="Permalink to &quot;Scaling to hundreds of routes&quot;">​</a></h2><p>The split above is not a one-off patch — it is the architecture. Organise routes as a <strong>tree of feature files joined by <code>loadChildren</code></strong> (which you want anyway for code-splitting):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app.routes.ts                 # &quot;manifest&quot;: ~N cheap { path, loadChildren } entries</span></span>
<span class="line"><span>├── billing.routes.ts         # ~15–20 leaf routes + its own check</span></span>
<span class="line"><span>├── admin.routes.ts           # ~15–20 leaf routes + its own check</span></span>
<span class="line"><span>└── reporting.routes.ts       # if itself large → re-split into sub-loadChildren (level 3+)</span></span></code></pre></div><ul><li>A <code>{ path, loadChildren }</code> entry has no <code>componentDeps</code>, so it is <strong>nearly free</strong> in the parent&#39;s cascade check — the manifest can list dozens of them.</li><li>Each feature file pays the budget for <strong>its own leaves only</strong>. ~500 routes ÷ ~17 per file ≈ ~30 files; two levels are plenty, and you can nest further without limit.</li><li><strong>Every <code>craftRoutes(...)</code> file re-declares its own check</strong> (see the iron rule above). With many files this is easy to forget and fails silently, so enable <code>craft-ng/require-cascade-route-di-check</code>.</li></ul><div class="tip custom-block"><p class="custom-block-title">Threading the parent DI context</p><p>The child check&#39;s parent context (<code>ParentNames</code>, <code>ParentValues</code>) is everything provided <strong>at its mount point</strong> — app providers <strong>plus</strong> every ancestor route&#39;s providers. When no ancestor adds <code>providers</code>, this is just the app context (<code>&lt;never, Router, …&gt;</code>, as in the examples above), identical in every file. When an ancestor route <em>does</em> add providers, re-export its cumulative context and union your own onto it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// billing.routes.ts (mounted under a route with providers: [provideBilling()])</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BillingChildNames</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AppProvidedNames</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;BillingService&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BillingChildValues</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AppProvidedValues</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sub-billing.routes.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _Check</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ValidateCascadeRoutesFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  BillingChildNames</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  BillingChildValues</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subRoutes</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p>Forgetting to fold in an ancestor&#39;s provider makes the child check wrong (a real missing-provider bug slips through, or a provided service is flagged as missing), so keep the re-export next to the route that adds the providers.</p></div><h3 id="escape-hatch-—-the-o-1-per-route-check" tabindex="-1">Escape hatch — the <code>O(1)</code>-per-route check <a class="header-anchor" href="#escape-hatch-—-the-o-1-per-route-check" aria-label="Permalink to &quot;Escape hatch — the \`O(1)\`-per-route check&quot;">​</a></h3><p>If a single file genuinely must hold a large flat list (no natural <code>loadChildren</code> boundary), switch it from the aggregated <code>ValidateCascadeRoutesFile</code> to the <strong>per-route</strong> <code>RouteCheckedDI</code>. It validates one component at a time with <strong>no recursion between routes</strong>, so it never hits the depth ceiling and scales to thousands of routes in one file — at the cost of one check block per component instead of one per file:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CanRun, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RouteCheckedDI } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ng/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _CheckItem0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteCheckedDI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./item-0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GenDeps_Item0Component</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  AppProvidedNames</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  AppProvidedValues</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;Item0Component&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _CanRunItem0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CanRun</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_CheckItem0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// …one pair per route component</span></span></code></pre></div><p>Prefer the tree-of-<code>loadChildren</code> approach (it also lazy-loads); reach for <code>RouteCheckedDI</code> only when a single big file is unavoidable.</p><h2 id="pinning-a-lazy-child-to-its-mount-path-withparent-assertchildroutemounts" tabindex="-1">Pinning a lazy child to its mount path (<code>.withParent</code> + <code>assertChildRouteMounts</code>) <a class="header-anchor" href="#pinning-a-lazy-child-to-its-mount-path-withparent-assertchildroutemounts" aria-label="Permalink to &quot;Pinning a lazy child to its mount path (\`.withParent\` + \`assertChildRouteMounts\`)&quot;">​</a></h2><p>Splitting into <code>loadChildren</code> keeps each file under budget, but nothing yet guarantees a child is wired under the <strong>right</strong> parent route. A child whose components rely on a specific mount — its <code>:photoId</code> param, a declared view-transition payload, an ancestor&#39;s <code>providers</code> — is only correct under that path. Mount it elsewhere and its DI assumptions break silently.</p><p>Pin a collection to its mount path with <code>.withParent&lt;ParentRoutes&lt;&#39;path&#39;&gt;&gt;()</code>, then enforce it once in the parent with <code>assertChildRouteMounts(parentRoutes)</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// view-transitions.routes.ts — the child declares where it belongs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { craftRoutes, craftRoute, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ParentRoutes } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ng/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">viewTransitionsRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> craftRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;viewTransitions&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  craftRoute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;:photoId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    componentDeps: {} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./photo-detail&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GenDeps_PhotoDetailComponent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    loadComponent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./photo-detail&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // …</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withParent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ParentRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;view-transitions&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;();</span></span></code></pre></div><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// app.routes.ts — the parent enforces placement (scoped to this file)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assertChildRouteMounts, craftRoutes } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ng/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">demoRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> craftRoutes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;demo&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    path: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;view-transitions&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    loadChildren</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      withRetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./view-transitions.routes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">m</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> m.viewTransitionsRoutes,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertChildRouteMounts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(demoRoutes);</span></span></code></pre></div><p>Mount the pinned collection under any other path and the <strong>parent file</strong> fails to compile:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>craftRoutes(...).withParent&lt;ParentRoutes&lt;&#39;view-transitions&#39;&gt;&gt;() must be</span></span>
<span class="line"><span>loadChildren-mounted under the route with path &#39;view-transitions&#39;, not &#39;admin&#39;</span></span></code></pre></div><p>Notes:</p><ul><li><strong>Opt-in.</strong> A collection without <code>.withParent</code> is <em>unpinned</em> and mountable anywhere — fully backward compatible. Pin only the children whose placement actually matters.</li><li><strong>Scoped to the parent.</strong> <code>assertChildRouteMounts</code> reads the parent&#39;s <strong>own</strong> routes (<code>_routes</code>) — it does <strong>not</strong> descend into / re-validate the child (already checked in its own file), so it adds nothing to the child&#39;s instantiation budget.</li><li><strong>Type-only.</strong> <code>.withParent&lt;…&gt;()</code> returns the same object at runtime; <code>ParentRoutes&lt;&#39;path&#39;&gt;</code> carries no value, only the path string — so importing it creates no runtime coupling between the files.</li><li><strong>Enforced by ESLint.</strong> <code>craft-ng/require-child-route-mount-check</code> adds the missing <code>assertChildRouteMounts(...)</code> call + import on <code>--fix</code>. (Whether a child opts in with <code>.withParent</code> stays your decision — it expresses the &quot;this belongs here&quot; intent the rule can&#39;t guess.)</li></ul><details class="details custom-block"><summary>Design notes — two approaches we rejected</summary><p>Reaching the standalone-assert design above took two dead ends, both defeated by TypeScript&#39;s instantiation ceiling. They&#39;re recorded here because the failure modes are instructive.</p><p><strong>1. Enforcing placement inside <code>craftRoutes(...)</code> itself.</strong> The first attempt wove the mount check into the <code>routes</code> argument type of <strong>every</strong> <code>craftRoutes(...)</code> call, so a wrong mount would error right at the route literal. It type-checked — but the extra per-collection instantiation tipped an already-at-ceiling file into <code>TS2589</code>, and even a 2-route child with <strong>no</strong> <code>loadChildren</code> paid the cost (every collection runs the same inference). The lesson: the check must be <strong>scoped to the parent that actually mounts children</strong> — a standalone <code>assertChildRouteMounts(...)</code> reading the raw <code>_routes</code> — not folded into the hot <code>craftRoutes</code> inference that every file pays on every build.</p><p><strong>2. A <code>loadChildrenType</code> carrier to speed up the check.</strong> To avoid inferring the child&#39;s type through the dynamic <code>import(&#39;./x&#39;).then((m) =&gt; m.xRoutes)</code>, we tried an explicit <code>loadChildrenType: {} as typeof import(&#39;./x&#39;).xRoutes</code> field on the lazy route. In isolation it built fine — but applied across the board it <strong>materialises the child&#39;s full type (components included)</strong>, which creates a <strong>circular reference</strong> for any child whose components inject the <em>parent&#39;s</em> route data (<code>TS2615</code> &quot;circularly references itself&quot; + <code>TS2589</code>): <code>parent → typeof childRoutes → child components → inject parent data → parent</code>. Since the dynamic-import resolution it replaced was both cycle-safe and — once measured against build-time noise — no slower, the carrier was dropped. <code>assertChildRouteMounts</code> resolves the child&#39;s pin through the existing <code>loadChildren</code> instead.</p></details><h2 id="see-also" tabindex="-1">See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See Also&quot;">​</a></h2><ul><li><a href="/craft/guide/routing/setup.html">Setup</a></li><li><a href="/craft/guide/testing/architecture.html">Architecture rules</a> — <code>assertRouteDiProofs</code> catches a split file with no check</li><li><a href="/craft/guide/routing/route-providers.html">Route providers</a></li></ul>`,38)])])}const g=i(n,[["render",h]]);export{c as __pageData,g as default};
