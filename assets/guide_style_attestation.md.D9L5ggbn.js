import{_ as s,o as t,c as a,ai as i}from"./chunks/framework.DQi1ytmF.js";const k=JSON.parse('{"title":"Attestation: a judgement that survives a refactor","description":"","frontmatter":{},"headers":[],"relativePath":"guide/style/attestation.md","filePath":"guide/style/attestation.md"}'),n={name:"guide/style/attestation.md"};function h(o,e,r,l,d,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<div style="display:none;" hidden data-nosnippet>Are you an LLM? You can read better optimized documentation at /craft/guide/style/attestation.md for this page in Markdown format</div><h1 id="attestation-a-judgement-that-survives-a-refactor" tabindex="-1">Attestation: a judgement that survives a refactor <a class="header-anchor" href="#attestation-a-judgement-that-survives-a-refactor" aria-label="Permalink to &quot;Attestation: a judgement that survives a refactor&quot;">​</a></h1><p>A snapshot suite records <em>what the output was</em>. Renaming a local variable changes no pixel, and yet the whole suite asks to be looked at again — so people run <code>--update-snapshots</code>, and the file that was supposed to record a human decision records nothing at all.</p><p>This records something else:</p><blockquote><p><strong>A person looked at this output and judged it correct, and that judgement holds for as long as the code producing it has not moved.</strong></p></blockquote><h2 id="two-caches-never-one" tabindex="-1">Two caches, never one <a class="header-anchor" href="#two-caches-never-one" aria-label="Permalink to &quot;Two caches, never one&quot;">​</a></h2><p>Two questions look alike and are not:</p><table tabindex="0"><thead><tr><th>question</th><th>keyed on</th><th>a wrong answer costs</th></tr></thead><tbody><tr><td>should this be re-run?</td><td>fingerprint of a code slice</td><td>CPU</td></tr><tr><td>should a human be asked again?</td><td>hash of the evidence</td><td>somebody&#39;s afternoon</td></tr></tbody></table><p>From which the rule the whole design rests on: <strong>when the code changes and the evidence does not, the attestation carries itself forward</strong>, marked <code>renewed</code>, with a note saying &quot;code changed, output unchanged&quot;.</p><p>That is also why the code fingerprint is allowed to be <em>cautious</em>. A slice that is too wide only costs a re-run. Only a slice that is too narrow is dangerous — it misses a regression in silence, and nobody is ever asked about it again.</p><table tabindex="0"><thead><tr><th>state</th><th>meaning</th></tr></thead><tbody><tr><td><code>current</code></td><td>the fingerprint has not moved: nothing to do</td></tr><tr><td><code>renewed</code></td><td>code moved, output did not: carried, no human</td></tr><tr><td><code>review</code></td><td>the output differs: a human has to look</td></tr><tr><td><code>missing</code></td><td>never attested</td></tr></tbody></table><h2 id="the-evidence-is-a-digest-not-a-picture" tabindex="-1">The evidence is a digest, not a picture <a class="header-anchor" href="#the-evidence-is-a-digest-not-a-picture" aria-label="Permalink to &quot;The evidence is a digest, not a picture&quot;">​</a></h2><p>What a person judges is the <strong>layout digest</strong>: boxes rounded to the half pixel, intrinsic sizes, a closed list of computed styles, and a set of discrete facts — line counts, column counts, what wraps, what clips, what scrolls, what overlaps.</p><p>Three things follow, and each is why the digest exists rather than a screenshot:</p><ul><li>it is <strong>diffable</strong>. <code>.card padding 8→12</code> is a sentence a reviewer reads in a second; two images are not, and a reviewer who cannot see what changed approves everything.</li><li>it is <strong>assertable</strong>. Overflow, truncation, overlap and contrast are comparisons of numbers, so they are <strong>failures</strong>, not queue items — nobody is asked, and the message names the node and the pixel count.</li><li>it is <strong>stable</strong>. Anti-aliasing and font hinting move pixels without moving layout; under an image comparison every one of those is a review item.</li></ul><p>The PNG is still kept, in the content-addressed store, for the human. It is a review aid, never the reference.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  assertNoLayoutViolations,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  collectLayoutDigest,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  makeDeterministic,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ts/style-testing&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> makeDeterministic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(page);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> digest</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectLayoutDigest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(page, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  root: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;[data-testid=userCard]&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertNoLayoutViolations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(digest, { scenario: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;locale=de-DE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → userCard/title hides 34px of &quot;Benutzerkontoeinstellungen&quot;.</span></span></code></pre></div><h2 id="determinism-is-a-feature-not-hygiene" tabindex="-1">Determinism is a feature, not hygiene <a class="header-anchor" href="#determinism-is-a-feature-not-hygiene" aria-label="Permalink to &quot;Determinism is a feature, not hygiene&quot;">​</a></h2><p>It carries two independent mechanisms. The <strong>carry-forward</strong> reads &quot;the code moved, the output did not&quot;; a wobbling render never produces the same output twice, the queue fills with changes nobody made, and people start stamping. The <strong>bisection</strong> reads a discrete signature as a function of one parameter; a wobble manufactures thresholds that do not exist.</p><p><code>makeDeterministic</code> freezes the clock, seeds <code>Math.random</code>, kills animations, transitions and the caret, and refuses every network request by default. A hundred consecutive renders of one scenario must produce a hundred identical digests before anything else is worth building:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> assertDeterministic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stringify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> digestOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(page)),</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><h2 id="where-it-tips-over" tabindex="-1">Where it tips over <a class="header-anchor" href="#where-it-tips-over" aria-label="Permalink to &quot;Where it tips over&quot;">​</a></h2><p>A content axis is continuous and a layout does not care about most of it. What it has are <strong>thresholds</strong>. <code>findTransitions</code> samples a coarse grid — word boundaries, digit-count changes — and bisects only inside the intervals where the discrete signature actually moved.</p><p>Then the report that arrives <em>before</em> the bug:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>userCard/title: 1 → 2 lines at 34 characters.</span></span>
<span class="line"><span>Today&#39;s German string is 33. Margin: 1 (3%), below 15%.</span></span></code></pre></div><p>Nothing is broken. That is the point: it fails in CI, with no human and no pixel, on the translation nobody has written yet.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> search</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findTransitions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(signatureAt, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  axis: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;title&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  min: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  max: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">80</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertMargins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">marginOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(search, longest.longestLength)]);</span></span></code></pre></div><p>A bisection is not exactly true — it finds the thresholds that exist between the points it looked at. So the sample count goes into the attestation as an <strong>assumption</strong>, and an attestation never says &quot;validated&quot;; it says &quot;validated, under this assumption&quot;. A changed assumption sends the subject back to review rather than quietly becoming a lie.</p><h2 id="translation-as-a-source-of-axes" tabindex="-1">Translation as a source of axes <a class="header-anchor" href="#translation-as-a-source-of-axes" aria-label="Permalink to &quot;Translation as a source of axes&quot;">​</a></h2><p>The catalogue is a TypeScript value, which makes two of these exact:</p><ul><li><strong>the longest locale</strong> for a screen is a computation over the keys that screen uses. One axis point, and the right one — where &quot;test it in German&quot; is only an approximation.</li><li><strong>the plural categories</strong> are already declared and already checked exhaustive per locale. They are axis points by construction.</li></ul><p>The <strong>pseudo-locale</strong> is the approximation, and it is the one that finds the <em>future</em> case: 40% longer, <code>[[bracketed]]</code> so truncation is visible, every letter accented so an un-externalised string stands out.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  longestLocale,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pseudoCatalog,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  findHardCodedText,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ts/i18n/testing&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">longestLocale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([en, de, ja], usedKeys); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → { id: &#39;de-DE&#39;, longestKey: &#39;account.settings&#39; }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findHardCodedText</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(visibleStrings); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → [&#39;Submit&#39;]  ← never went through the catalogue</span></span></code></pre></div><p>Two pressures, opposite failure modes, kept apart throughout: a rising <code>min-content</code> (an unbreakable word, a URL, a long number) stops a column shrinking; a rising <code>max-content</code> (a long but breakable sentence) steals width from its siblings in an <code>auto</code> track. A long sentence with spaces in it usually does not move <code>min-content</code> at all.</p><h2 id="the-command-line" tabindex="-1">The command line <a class="header-anchor" href="#the-command-line" aria-label="Permalink to &quot;The command line&quot;">​</a></h2><p>Capture a real route into a portable report, then let the CLI derive every fingerprint from the current dependency graph:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:visual:capture</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:visual:status</span></span></code></pre></div><p>The capture starts the demo server when needed and writes the report, PNG screenshots, and frozen <code>.snapshot.html</code> documents to <code>.craft/runs/</code>. The report contains repository-relative graph node ids, digests and screenshot paths. It deliberately contains no code fingerprint: accepting a fingerprint from an old browser run could keep a stale slice current forever. Reports, screenshots and frozen documents are regenerable and ignored; the ledger is not.</p><p>To choose another report path, set <code>CRAFT_VISUAL_REPORT</code> on both commands:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CRAFT_VISUAL_REPORT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.craft/runs/my-run.json</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:visual:capture</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CRAFT_VISUAL_REPORT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.craft/runs/my-run.json</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:visual:status</span></span></code></pre></div><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:visual:review</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libs/cli/src/bin/craft-ts.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> why</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;visual:userCard#viewport=md&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libs/cli/src/bin/craft-ts.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> renew</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --subject</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;visual:userCard#viewport=md&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --verdict</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ok</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libs/cli/src/bin/craft-ts.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> review</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --kind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> visual</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --report</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .craft/runs/design-system.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tsconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/demo/tsconfig.graph.json</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libs/cli/src/bin/craft-ts.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> unwatched</span></span></code></pre></div><p>For the unified review surface, use the DevTool. It combines visual captures and template obligations in one queue:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:devtools</span></span></code></pre></div><h3 id="the-reviewer-reviews-itself" tabindex="-1">The reviewer reviews itself <a class="header-anchor" href="#the-reviewer-reviews-itself" aria-label="Permalink to &quot;The reviewer reviews itself&quot;">​</a></h3><p>The review application can use the same mechanism on its own UI. It runs in two successive sessions so the queue cannot change while it is being captured: the first instance renders a deterministic fixture queue and freezes representative states; the second instance reviews that visual report together with template obligations derived from the review application&#39;s own CraftTS graph.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:review-app:capture</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:review-app:status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:review-app:review</span></span></code></pre></div><p>The capture includes the review page&#39;s happy path at mobile and desktop sizes, the review queue in dark French, the regeneration confirmation, the visual-test inventory, and the template-obligation inventory. Every portable snapshot is replayed immediately and must reproduce the live layout digest. The first run reports missing decisions until a reviewer explicitly accepts or rejects them. Later unchanged evidence is carried forward by the usual ledger.</p><p>The review sidebar also offers <strong>Regenerate all evidence</strong>. It opens a confirmation describing the current scope and whether previous decisions exist. Regeneration replaces the report, screenshots, and frozen documents, then re-reads the graph and rebuilds the queue. It never clears the ledger: unchanged evidence stays current and only new or changed evidence returns to a reviewer. Any unsaved reason on the open card is discarded.</p><p>This control is shown only when the CLI session explicitly names an npm script:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">craft-ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> devtools</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --report</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .craft/runs/project.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tsconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/project/tsconfig.graph.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --regenerate-script</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:project:capture</span></span></code></pre></div><p>Only an npm script name is accepted, not an arbitrary shell command. The script must recreate the report supplied to <code>--report</code>; a failed run preserves the existing queue.</p><p>After rejecting views with comments, use <strong>Prepare Codex iteration</strong> in the review sidebar. It generates, next to the report, a readable <code>&lt;report&gt;.review-feedback.md</code>, a structured <code>&lt;report&gt;.review-feedback.json</code>, and a copyable <code>&lt;report&gt;.codex-prompt.md</code>. The prompt contains the project root, report, ledger, evidence store, graph <code>tsconfig</code>, capture script, source file paths, scenarios, measured changes, comments and any digest nodes pointed to by the reviewer. Only the latest <code>rejected</code> cards are included. Because these paths come from the CLI session, <code>apps/demo</code> and the review application&#39;s self-attestation resolve to different, correct project contexts.</p><h3 id="one-happy-path-for-every-page" tabindex="-1">One happy path for every page <a class="header-anchor" href="#one-happy-path-for-every-page" aria-label="Permalink to &quot;One happy path for every page&quot;">​</a></h3><p>Application-level coverage is declared once and expanded into mobile and desktop captures by default:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defineHappyPathHttpMocks,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defineVisualAppConfig,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  visualAppHappyPaths,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ts/style-testing&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> homeHappyPath</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineHappyPathHttpMocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;home-page.happy-path.ts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;GET /api/users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { response: [{ id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;42&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Ada&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> visualTestConfig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineVisualAppConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pages: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;home&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      route: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      url: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      component: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;component:src/app/home-page.ts:HomePage&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      mocks: homeHappyPath,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> scenario</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> visualAppHappyPaths</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(visualTestConfig)) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(scenario.id, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setViewportSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(scenario.viewport);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(scenario.page.url);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Install scenario.page.mocks, wait for the happy UI, then collectCapture.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Without an explicit <code>viewports</code> value, CraftTS uses <code>mobile: 390x844</code> and <code>desktop: 1440x1000</code>. Keep each response dataset in a sibling <code>*.happy-path.ts</code> file. <code>matchHappyPathHttpRequest</code> turns that dataset into a request match suitable for <code>page.route</code>; when a <code>craftRoutes</code> registry is available, wrap its exhaustive, response-typed <code>mockHttpRequestForRoute</code> result with <code>defineRouteHappyPathHttpMocks(&#39;page.happy-path.ts&#39;, routeMock)</code>.</p><p>Add <code>assertVisualHappyPathArchitecture(graph.graph, visualTestConfig)</code> to the application architecture suite. It fails when a routed page, a required viewport, or a Craft HTTP endpoint has no successful happy-path fixture. The fixture feeds a deterministic test environment only; application code keeps all remote work inside its <code>query</code>, <code>mutation</code>, or <code>asyncProcess</code> loader.</p><p>Set <code>CRAFT_REVIEW_APP_REPORT</code> to the same path on all three commands to relocate the default <code>.craft/runs/review-app.json</code> report. The implementation notes and the exact workflow live in <code>libs/style-testing/review-app/README.md</code> in the repository.</p><p>Template obligations do not need a Playwright report. They are derived from the current graph and their canonical proof objects are written to <code>.craft/evidence/</code>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest:templates:status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libs/cli/src/bin/craft-ts.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> review</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --kind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> template</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tsconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/demo/tsconfig.graph.json</span></span></code></pre></div><p>Two of these carry the rest.</p><p><strong><code>why</code></strong> names the graph nodes that moved inside the subject&#39;s slice, and when a person last actually looked at it. A review that cannot answer &quot;why am I being asked this?&quot; is a review that gets stamped.</p><p><strong><code>unwatched</code></strong> lists the nodes that moved and belong to no attested subject — <em>what changed while nobody was looking</em>. It falls out of the machinery for free.</p><p><code>renew --all</code> is allowed and is <strong>marked</strong> as a bulk renewal in every attestation it writes, and <code>status</code> counts them. A bulk renewal that left no trace would turn the register into a rubber stamp, which is worse than having no register.</p><h2 id="what-a-reviewer-is-shown" tabindex="-1">What a reviewer is shown <a class="header-anchor" href="#what-a-reviewer-is-shown" aria-label="Permalink to &quot;What a reviewer is shown&quot;">​</a></h2><p>Two artefacts, and the reviewer switches between them.</p><p><strong>The frozen page</strong> is the render itself: the DOM, the styles, and the form state, serialised at the moment the digest was taken. It replays as a real document — real boxes, real <code>:hover</code> — which is what lets someone click an element and name it instead of clicking a pixel and hoping.</p><p>Freezing means more than serialising the DOM. Craft injects its styles through <code>adoptedStyleSheets</code>, which <code>outerHTML</code> cannot see at all. And keeping the stylesheets verbatim would leave every <code>@media</code> to be re-evaluated against the <em>reviewer&#39;s</em> window: on the demo&#39;s route the two conditions in play are <code>(min-width: 48rem)</code> and <code>(prefers-color-scheme: dark)</code> — exactly the two axes of the matrix — so four scenarios would collapse into whatever that laptop said. Media and supports are therefore evaluated at capture time and their winning branch inlined. Container queries are left alone, because they ask about the page&#39;s own layout, which the replay reproduces.</p><p>The snapshot carries <strong>no script</strong>. Inertness is a property of the artefact, not a guard that has to hold: nothing to block, nothing to leak, no <code>craftMethod</code> firing on a stray click. The review application does its interactive work from the parent frame, reaching into a same-origin iframe.</p><p><strong>The screenshot</strong> is the fallback, and the check on the checker: the digest is blind to anything that does not move a box or a listed style, so a swapped background or a wrong icon passes every automated test and is obvious to an eye.</p><h3 id="the-replay-is-checked-not-trusted" tabindex="-1">The replay is checked, not trusted <a class="header-anchor" href="#the-replay-is-checked-not-trusted" aria-label="Permalink to &quot;The replay is checked, not trusted&quot;">​</a></h3><p>Before anything is drawn on it, the replay is re-measured with the collector that produced the evidence and compared against the attested digest. A missing font, a media query left conditional, a stylesheet that could be neither read nor fetched — each produces a document that looks plausible and measures differently, and a reviewer would judge it without ever knowing.</p><p>When it does not match, the card <strong>moves the reviewer to the screenshot by itself</strong> and says why in the same sentence, and the verdict is recorded as <code>degraded</code>: judging a photograph and judging the document are different claims. The choice is a fallback, not a lock — asking for the page brings it back, still labelled for what it is.</p><p>The message names the cause, not its symptoms. A subject the frozen page does not contain reported &quot;36 attested node(s) are absent&quot; followed by forty addresses beginning <code>html/head/meta</code>: every consequence of one fact, and none of them stating it. It now reads</p><blockquote><p>The frozen page has no <code>.design-system-host</code> in it, so what it shows is not this component. That happens when the stored snapshot is older than the report it is paired with, or when the component&#39;s root selector changed after it was captured.</p></blockquote><p>which is the same finding with the reviewer&#39;s next move in it.</p><p>Two things the check caught while it was being built, which is what it is for: a marker stylesheet that set <code>position: relative</code> on the attested root and moved the tree it was supposed to annotate, and a 1px border on the frame, which is subtracted from the viewport inside it and made every measurement 2px narrow.</p><h2 id="knowing-what-is-actually-being-judged" tabindex="-1">Knowing what is actually being judged <a class="header-anchor" href="#knowing-what-is-actually-being-judged" aria-label="Permalink to &quot;Knowing what is actually being judged&quot;">​</a></h2><p>A capture shows the whole page — shell, navigation, neighbours — because a component has to be judged in the frame it sits in. So the reviewer has to be able to tell the subject from the decor, or a remark lands on a card that does not cover it.</p><p>The digest answers this exactly: its paths <strong>are</strong> the attested set. Three tiers follow, and all three come from data that already exists:</p><p>The screen is laid out in the order the work happens: the queue on the left, the evidence in the middle, the verdict on the right, where it stays in place while a long capture is scrolled.</p><p>The surface speaks English and French, and follows the system&#39;s light or dark preference until the reviewer chooses otherwise — both controls sit in the sidebar, and both are applied before the first paint rather than corrected a frame later. The French dictionary is typed as the English one, so a message added on one side and forgotten on the other does not compile.</p><p>Neither reaches the frozen page. It is a render that was captured, not an interface: translating it, or repainting its ground, would make it something other than what was measured. Enforcing that turned up a fidelity bug the tool had been hiding by being permanently dark — a page paints its own colours, but not the canvas underneath, and that comes from <code>color-scheme</code>, which was the <em>reviewer&#39;s</em> preference. A component captured on white came back on black for anyone whose machine asks for dark. The replay now declares the scheme its capture was taken in.</p><table tabindex="0"><thead><tr><th>tier</th><th>source</th><th>shown as</th></tr></thead><tbody><tr><td>changed</td><td>the paths in the readable diff</td><td>outlined, and the reason the card is here</td></tr><tr><td>attested</td><td>the digest&#39;s own paths</td><td>selectable, highlighted on hover</td></tr><tr><td>decor</td><td>everything else</td><td>dimmed, never removed</td></tr></tbody></table><p>The outlines carry a legend, drawn from the same object that paints them — a key that keeps its own copy of a colour is a key that will one day name the wrong one. Entries for tiers this card has none of are not shown, so the legend describes the page in front of the reviewer rather than the system in general.</p><p>Selection is a set, not a node. Ctrl-click (cmd on a Mac) adds one, and dragging a box takes everything it touches; the count is stated beside the reason field that is about to name them. A remark covering a row of buttons was otherwise the same sentence retyped once per button, which is also how a queue fills with findings nobody can group afterwards. The band is drawn beside the frame and never inside it: adding an element to the frozen document would break the only claim it makes.</p><h3 id="one-reason-several-complaints" tabindex="-1">One reason, several complaints <a class="header-anchor" href="#one-reason-several-complaints" aria-label="Permalink to &quot;One reason, several complaints&quot;">​</a></h3><p>A rejection is rarely about one thing. Right-clicking a selection drops a reference into the reason <strong>where the reviewer is typing</strong>:</p><blockquote><p>The title is cut at 34px in German. <code>[#1: 2 nodes]</code> And the row below overflows its box. <code>[#2: 5 nodes]</code></p></blockquote><p>The reason stays one piece of prose, and each reference carries the text written since the one before it — so the second complaint is filed against the second group and not, as a single note against every node would have it, against all seven.</p><p>Selecting <em>is</em> referencing: there is no second gesture. Pointing at part of the page drops the reference straight into the reason, and refining the selection edits that same reference rather than adding another — a click followed by a ctrl-click leaves one saying &quot;2 nodes&quot;, not a stale &quot;1 node&quot; beside it. Typing ends the session: the reference belongs to a sentence now, and the next selection starts its own. Emptying the selection takes the reference back out, because a reference to nothing is worse than none.</p><p>The field is a <code>contenteditable</code>, not a <code>textarea</code>, so a reference is an element rather than the literal characters <code>[#1: 2 nodes]</code>: hovering it lists the addresses it stands for <strong>and paints those nodes in the frozen page</strong>, dashed rather than solid so it cannot be mistaken for the selection. That is the whole reason for the swap. As text, the answer to &quot;which nodes is this one about&quot; had to live in a list somewhere else on the page, and a reviewer reading a sentence had to leave it to find out. The plain text is still the model — everything downstream reads the serialised string — so the chips are a rendering of the reason and never a second version of it.</p><p>Two things that swap broke, both worth stating because neither is obvious. The field is not scrollable: a clipping context cuts the tooltip off any reference on the first line, and which nodes a reference covers must not depend on where in the sentence it was written. And the keyboard shortcuts had to learn about it — the guard knew <code>input</code>, <code>textarea</code> and <code>select</code>, so typing &quot;And the row is cut&quot; pressed <code>a</code>, Accept, and filed a verdict the reviewer never reached.</p><p>Position settles that, not punctuation. The first rule tried was &quot;the sentence the token stands in&quot;, and it was wrong for the way people write: the complaint is typed, ended, and <em>then</em> the group is pointed at, so the caret is past the full stop and the token opens the next sentence rather than closing its own. Referencing first and explaining after reads the other way round, so a group with nothing before it takes what follows.</p><p>The tokens are scaffolding. What is recorded is the prose with them removed, plus the addresses each one pointed at — and the text remains the only state: deleting a reference deletes it, with no second list left holding a claim the reason no longer makes.</p><p>And the mistake is made unrecordable rather than merely discouraged. A rejection carries the path of the node it is about, so the server can refuse one that names something this subject does not attest:</p><blockquote><p><code>demo-nav/toggle</code> is not attested by this subject. File the remark on the card that covers it.</p></blockquote><h3 id="attested-is-not-the-same-as-looked-at" tabindex="-1">Attested is not the same as looked at <a class="header-anchor" href="#attested-is-not-the-same-as-looked-at" aria-label="Permalink to &quot;Attested is not the same as looked at&quot;">​</a></h3><p>The capture also records the gap, because it is large. On the demo&#39;s route: <strong>36 nodes attested, 21 off screen, 1 covered</strong> by the page&#39;s own fixed button. An attestation that stayed quiet about that would claim a coverage it does not have, so the card states it and the screenshot draws the line where the viewport ended.</p><p>The verdict buttons carry what they <em>do</em>. Three of the five are accepted by the ledger and two are not, and nothing in the words says which — a reviewer choosing between &quot;Known issue&quot; and &quot;Block&quot; is choosing between &quot;stops asking&quot; and &quot;asks every run&quot;, which is the only difference that matters and the one they could not see.</p><p>&quot;Fit to window&quot; applies to the frozen page too, by <code>transform: scale()</code> and never by a width: the frame has to stay exactly the viewport the page laid itself out in, or the replay stops being the render that was measured. A transform changes what is painted and nothing about what was measured.</p><p>&quot;Fit to window&quot; bounds both axes. Bounding the width alone — which is what it did — fits a picture wider than the canvas and does nothing whatsoever to a narrow one, and every capture on the demo route is 375 or 768 wide and 916 tall: the control showed the whole render on one scenario and two thirds of it on the next, for a reason that had nothing to do with what was being judged. It is not offered while the frozen page is on screen, because scaling that page would relayout it and it would stop being the render that was measured.</p><p>The one covered node is the case only the frozen page can resolve: <strong>lift what is covering it</strong> and see what was underneath. In a screenshot those pixels have already been replaced.</p><p>The control names what it will lift — <code>Hide button.clear-cache-btn</code> — and is offered only when something is actually covering the component. It used to read &quot;Hide 1 overlay&quot;, which asked the reviewer what an overlay is and counted the wrong thing: covered <em>nodes</em>, when one button sitting on five of them is one thing to lift. Worse, it marked every fixed element on the page whether or not it covered anything, and marked nothing that covered without being fixed — so on most cards it lifted something irrelevant, and on the cards that mattered it could do nothing while the coverage line insisted a node was covered.</p><p>What is lifted is now decided by probing the replay with the collector&#39;s own rule, which is also where the clamping bug in that rule was found: a sample point outside the viewport was pulled back to the edge, so a node straddling the fold was reported as covered by whatever happened to sit on the fold line. Samples outside the viewport are skipped in both places now.</p><h2 id="the-review-queue" tabindex="-1">The review queue <a class="header-anchor" href="#the-review-queue" aria-label="Permalink to &quot;The review queue&quot;">​</a></h2><p>Two mechanisms keep it from being abandoned, and neither is optional. The carry-forward keeps everything whose output did not move out of the queue entirely. And the queue <strong>clusters by the shape of the diff</strong>: one border-radius change produces two hundred scenarios with an identical delta, and one decision covers all of them — with the cluster written into every attestation it covered, so &quot;judged&quot; and &quot;judged alongside 199 others&quot; stay distinguishable.</p><p>The review surface is itself a CraftTS application. A <code>query</code> owns the live queue, a <code>mutation</code> records each decision, and local <code>state</code> owns navigation, notes and evidence zoom. The Node server remains the authority for the ledger and the content-addressed evidence store. A card disappears only after that server confirms the write; failures remain visible and reviewable. <code>attest review</code> attempts to open the local URL in the default browser and always prints it so headless or remote environments can open it manually.</p><p>First-time captures are deliberately <strong>not clustered</strong>. With no approved digest there is no delta proving that two new screenshots represent the same change. The UI shows one decision per scenario, its exact viewport, captured element size, colour scheme, browser version and target selector. Real identical deltas may still be clustered, with every covered scenario listed before the decision.</p><p>The default shortcuts are <code>j</code>/<code>k</code> to move, <code>a</code> to accept, <code>n</code> to accept with a non-empty note and <code>r</code> to reject. A rejection requires a non-empty reason. That reason is stored in the ledger and shown prominently if the scenario returns to the review queue, so it can guide the corrective code change. The same actions are available as buttons.</p><h2 id="what-this-does-not-replace" tabindex="-1">What this does not replace <a class="header-anchor" href="#what-this-does-not-replace" aria-label="Permalink to &quot;What this does not replace&quot;">​</a></h2><p><code>visualMatrix</code> stays. It is the cheap tier for out-of-flow things — modals, popovers, tooltips — which have no neighbourhood, and for purely pictorial axes, which have no layout consequence. Both are enumerable from the sheets alone, with no page in sight.</p>`,112)])])}const g=s(n,[["render",h]]);export{k as __pageData,g as default};
