import{_ as t,o as s,c as a,ai as i}from"./chunks/framework.DQi1ytmF.js";const k=JSON.parse('{"title":"Activating @craft-ts/style","description":"","frontmatter":{},"headers":[],"relativePath":"guide/style/setup.md","filePath":"guide/style/setup.md"}'),n={name:"guide/style/setup.md"};function h(l,e,d,r,o,p){return s(),a("div",null,[...e[0]||(e[0]=[i(`<div style="display:none;" hidden data-nosnippet>Are you an LLM? You can read better optimized documentation at /craft/guide/style/setup.md for this page in Markdown format</div><h1 id="activating-craft-ts-style" tabindex="-1">Activating <code>@craft-ts/style</code> <a class="header-anchor" href="#activating-craft-ts-style" aria-label="Permalink to &quot;Activating \`@craft-ts/style\`&quot;">​</a></h1><p>The typed style system is not a runtime library you import and call. It is a <strong>build step</strong>: a Vite plugin evaluates every <code>*.style.ts</code> in Node, deduplicates what they registered, and emits one stylesheet. Without that plugin the vocabulary still typechecks and still compiles — and the page renders with no CSS at all.</p><p>This page is the one to follow before the other four.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @craft-ts/style</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --save-dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @craft-ts/style-testing</span></span></code></pre></div><p><code>@craft-ts/style</code> carries the vocabulary — tokens, kinds, typed custom properties, axes, sheets, obligations. <code>@craft-ts/style-testing</code> carries the scenario matrix and the drivers that reach each of its points; it never ships to the browser, so it belongs in <code>devDependencies</code>.</p><p><code>@craft-ts/style</code> declares <code>@craft-ts/core</code> as a peer dependency, and <code>@craft-ts/style-testing</code> declares <code>@craft-ts/style</code>. Both are <code>sideEffects: false</code>.</p><h2 id="wire-the-plugin" tabindex="-1">Wire the plugin <a class="header-anchor" href="#wire-the-plugin" aria-label="Permalink to &quot;Wire the plugin&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineConfig } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { craftStyle } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ts/style/vite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    craftStyle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Written on every emission, so the graph never reads a picture of the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // sheet that is older than the CSS the browser got.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      dumpPath: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tmp/craft-style-graph.json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>craftStyle</code> takes four options, all optional:</p><table tabindex="0"><thead><tr><th>option</th><th>default</th><th>what it decides</th></tr></thead><tbody><tr><td><code>suffix</code></td><td><code>&#39;.style.ts&#39;</code></td><td>the filename suffix that marks a module as a sheet</td></tr><tr><td><code>ignore</code></td><td><code>[&#39;node_modules&#39;, &#39;dist&#39;, &#39;.git&#39;, &#39;.nx&#39;, &#39;tmp&#39;]</code></td><td>directory names the walk never descends into</td></tr><tr><td><code>dumpPath</code></td><td>none — no dump is written</td><td>where to write the graph dump</td></tr><tr><td><code>alias</code></td><td>none</td><td>module aliases for the <strong>Node</strong> evaluation of the sheets</td></tr></tbody></table><p><code>alias</code> exists because the sheets are evaluated by a real bundler in a separate pass, before your app&#39;s own resolution applies. In a published project, Node resolution finds <code>@craft-ts/style</code> on its own and you can leave <code>alias</code> out. In this monorepo the demo passes the workspace source paths — see <a href="https://github.com/craft-ts/craft-ts/blob/main/apps/demo/vite.config.ts" target="_blank" rel="noreferrer"><code>apps/demo/vite.config.ts</code></a>, which is the working reference for everything on this page.</p><p>Then import the emitted sheet once, at the app entry:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;virtual:craft-style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>If your <code>tsconfig</code> does not already know that id, declare it next to your other ambient types:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> module</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;virtual:craft-style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h2 id="what-the-plugin-produces" tabindex="-1">What the plugin produces <a class="header-anchor" href="#what-the-plugin-produces" aria-label="Permalink to &quot;What the plugin produces&quot;">​</a></h2><p>Two artefacts, from one evaluation.</p><p><strong>The CSS.</strong> Every <code>when(...)</code> and <code>set(...)</code> the sheets registered, rendered as atomic rules and <code>@property</code> registrations, deduplicated across files, and served under <code>virtual:craft-style.css</code>. This is the whole stylesheet: no class is ever assembled in the browser, so what the browser gets is exactly what the emitter proved.</p><p><strong>The dump</strong>, when <code>dumpPath</code> is set. A JSON picture of the registry — classes, atoms, and typed variables — written on <em>every</em> emission, so it can never describe a sheet older than the CSS that was served alongside it. The dump is the style half of the dependency graph: it is what <a href="./testing.html#what-the-graph-adds"><code>style_impact</code>, <code>style_matrix</code> and <code>style_debt</code></a> read, and what the <code>@craft-ts/dev-tools</code> style queries read.</p><p>The plugin re-derives the whole sheet when a <code>*.style.ts</code> changes rather than patching it. Atomic output is small and the emission is one bundle away; an incremental path here would be a second source of truth about what the CSS says.</p><h2 id="what-breaks-without-it" tabindex="-1">What breaks without it <a class="header-anchor" href="#what-breaks-without-it" aria-label="Permalink to &quot;What breaks without it&quot;">​</a></h2><table tabindex="0"><thead><tr><th>missing</th><th>symptom</th></tr></thead><tbody><tr><td>the plugin</td><td>no CSS at all — the classes exist as strings, nothing ever wrote their rules</td></tr><tr><td><code>import &#39;virtual:craft-style.css&#39;</code></td><td>same, and less obviously: the plugin runs but nothing pulls its output</td></tr><tr><td><code>dumpPath</code></td><td><code>style_matrix</code> and the other graph queries have nothing to read and say so</td></tr></tbody></table><p>The MCP server names the fix in its own error message: it points you back at <code>craftStyle({ dumpPath })</code>. If you are reading this because you saw that message, <code>dumpPath</code> is the line you are missing.</p><h2 id="emitting-without-a-vite-server" tabindex="-1">Emitting without a Vite server <a class="header-anchor" href="#emitting-without-a-vite-server" aria-label="Permalink to &quot;Emitting without a Vite server&quot;">​</a></h2><p><code>@craft-ts/style/vite</code> exports the emitter itself, so a test or a script can get the same two artefacts without standing up a dev server:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { emitStyles } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@craft-ts/style/vite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The same two artefacts the plugin produces, without a Vite server: \`css\` is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// what the browser gets, \`dump\` is what the graph and the MCP tools read.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> emitOnce</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  emitStyles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;src/app/ui/button.style.ts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;src/app/ui/foundation.style.ts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span></code></pre></div><p><code>vite</code> is a peer of that entry point, not a dependency: a project that builds with something else can still call <code>emitStyles</code> without pulling Vite&#39;s types into its own program.</p><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to &quot;Next&quot;">​</a></h2><ul><li><a href="./define.html">Define your design system</a> — palette, axes, theme: where <code>bp</code>, <code>scheme</code> and <code>palette</code> come from.</li><li><a href="./tokens.html">Tokens and typed variables</a> — level 1.</li><li><a href="./variants.html">Axes and the visual matrix</a> — level 2.</li></ul>`,31)])])}const g=t(n,[["render",h]]);export{k as __pageData,g as default};
